export enum FileActionOptions {
    Delete = 'delete',
    Download = 'download',
    Open = 'open',
    Accept = 'accept',
    Return = 'return',
    Process = 'process',
    Publish = 'publish',
    Decline = 'decline',
    AddToDoc = 'addToDoc',
    RemoveFromDoc = 'removeFromDoc',
    Archive = 'archive',
    CopyLink = 'copyLink',
    Extract = 'extract'

    // Approve = 'approve',
    // Move = 'move',
    // Start = 'start',
    // Copy = 'copy',
    // Rename = 'rename'
}